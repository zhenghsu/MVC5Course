@model Nullable<double>
           @if (Model.HasValue)
           {
              for(int i=0;i<Model.Value;i++)
               {
                <img src="~/img/star.png" />
                     }
                     }