@model String
           @if (Model=="M")
           {
               @:男性
           }
           else if (Model == "F")
           {
               @:女性 
           }
           else if(Model==null)
           {
           @:不確定
           }